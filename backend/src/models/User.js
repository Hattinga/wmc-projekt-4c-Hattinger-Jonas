// TODO: User-Model – DB-Zugriff für users-Tabelle.
// Exportiert:
//   User.create({ username, email, passwordHash })  → neuer User
//   User.findByEmail(email)                          → User oder null
//   User.findById(id)                                → User oder null
//   User.update(id, fields)                          → aktualisierter User
//   User.delete(id)                                  → void
