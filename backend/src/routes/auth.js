// TODO: Auth-Routen
// POST /register  – User erstellen (bcrypt hash, User.create())
// POST /login     – Credentials prüfen (bcrypt.compare), JWT signieren + zurückgeben
// GET  /me        – Eigenes Profil (auth-Middleware erforderlich)
// PATCH /me       – Profil aktualisieren (username, email, passwort)
