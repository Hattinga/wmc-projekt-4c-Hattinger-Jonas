// TODO: Graph-Daten-Route (auth-Middleware)
// GET / – Gibt alle Nodes (Notizen: id, title) und Edges (note_links: source_id, target_id)
//         des eingeloggten Users zurück.
//         Format: { nodes: [{id, title}], edges: [{source, target}] }
