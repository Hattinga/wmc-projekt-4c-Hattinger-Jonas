# Meilenstein-Plan vs. Ist-Fortschritt

| KW | Zeitraum | Meilenstein (Soll) | Ist-Stand | Status |
|----|----------|-------------------|-----------|--------|
| 20 | 12.–18.05. | Konzept + Setup | Konzept abgegeben ✅ | ✅ |
| 21 | 19.–25.05. | Auth + Basis-UI | Frontend-UI vollständig (Auth-Seite, Dashboard, Editor, Graph, Settings als Svelte 5 Komponenten). **Backend-Logik war Stub** – wurde in KW 22 nachgeholt. | ⚠️ |
| 22 | 26.05.–01.06. | CRUD + Editor | **KW-21-Backfill:** Auth-Backend + Frontend-Auth-Wiring. **Notes CRUD:** `Note.js` Model, `GET/POST/PUT/DELETE /api/notes`, Dashboard + Detailseite verdrahtet, Autosave. **Ordner-System:** `Folder.js` Model, `GET/POST/PUT/DELETE /api/folders`, Sidebar lädt echte Ordner per API. **Bugfix:** Wiki-Link-Navigation. **Bug-Fix-Sweep (59 Funde):** XSS-Schutz, Autosave-Race, rekursiver Trigger, 401-Auto-Logout, Auth-Flash, Folder-Ownership, Circular-Parent, Cross-Tab-Logout, Dynamic-SQL für null-fähige Felder u.v.m. | ✅ |
| 23 | 02.–08.06. | Links + Graph + WS | **Alle KW23-Features implementiert (Commit 03.06.):** `syncLinks()` aktualisiert `note_links` bei jedem Save; `GET /notes/:id/backlinks` + Panel-Anbindung; Tags-CRUD (Model, Routen, `note_tags`-Join); `GET /api/graph` + D3-Force-Graph (Drag, Zoom, Degree-Sizing); Socket.io mit JWT-Auth und User-Rooms (`note:updated`/`note:deleted`); Settings verdrahtet. Per Smoke-Test am 09.06. end-to-end verifiziert. Bekanntes Manko: kein WS-Event bei Note-*Erstellung* → Fix in KW 24. | ✅ |
| 24 | 09.–11.06. | Polish + Abgabe | **09.06.:** Smoke-Test + Frontend-Audit (3 Subagents), Quick-Fixes (Root-Redirect, Ordner-UI, Notiz-Löschen, WS-`note:created`), tote Stubs entfernt. **10.06.:** Responsive Layout (Sidebar-Slide-in, gestackter Editor, Dashboard/Settings mobil), i18n-Extraktion (~130 Strings, Sprachumschalter wirksam), Tag-UI im Editor, toter Graph-Filter entfernt. **11.06.:** Test-Checkliste komplett (Smoke-Test-Skript mit 26 API/WS-Checks, alle ✅, kein Blocker), Doku-Abschluss, finaler Push. **Abgabe erfolgt.** | ✅ |
