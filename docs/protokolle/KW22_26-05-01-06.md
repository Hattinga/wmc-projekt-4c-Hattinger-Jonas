# Wochenprotokoll KW 22 (26.05. – 01.06.2026)

## Was wurde gemacht?

- **KW-21-Backfill – Backend-Auth vollständig implementiert:**
  - Audit ergab: alle 12 Backend-Quelldateien waren reine TODO-Kommentar-Stubs (0 % implementiert)
  - `backend/src/config/database.js`: better-sqlite3-Verbindung, Schema-Bootstrap beim ersten Start (`IF NOT EXISTS`), `PRAGMA foreign_keys = ON`
  - `backend/src/models/User.js`: `create`, `findByEmail`, `findById`, `update`, `delete` mit prepared statements; SQLITE_CONSTRAINT_UNIQUE wird als `DUPLICATE`-Error weitergegeben
  - `backend/src/middleware/auth.js`: `Authorization: Bearer`-Header parsen, `jwt.verify` mit JWT_SECRET, `req.user` setzen, 401 bei fehlendem oder ungültigem Token
  - `backend/src/routes/auth.js`: `POST /register` (Validierung, bcrypt-Hash, User anlegen, JWT zurückgeben), `POST /login` (bcrypt.compare, JWT), `GET /me`, `PATCH /me`; Antwortformat immer `{ token, user }` ohne `password_hash`
  - `backend/src/index.js`: Express-App, CORS, dotenv, JSON-Body-Parser; nur `/api/auth` gemountet — andere Routen folgen in KW 22/23 wenn implementiert
  - `backend/package.json`: `dotenv ^16.4.0` ergänzt; `better-sqlite3` von v9 auf v12 aktualisiert (v12 hat prebuilt-Binaries für Node.js 24 — v9 schlägt auf diesem System ohne C++-Toolset fehl)
  - `backend/db/seed.sql`: Placeholder-bcrypt-Hash durch echten Hash für `password123` ersetzt
- **Frontend-Auth verdrahtet:**
  - `frontend/src/lib/stores/appState.svelte.js`: `currentUser: null` hinzugefügt; `locale` und `currentUser` werden beim Start aus `localStorage` geladen (war vorher nicht implementiert)
  - `frontend/src/routes/auth/+page.svelte`: `handleLogin`/`handleRegister` rufen jetzt `api.login`/`api.register` auf, schreiben Token + User in `localStorage`, setzen `appState.currentUser`
  - `frontend/src/routes/+layout.svelte`: `$effect`-Auth-Guard — leitet ohne Token auf `/auth` weiter
  - `frontend/src/lib/services/api.js`: Fehlerauswertung liest jetzt `err.error || err.message` (Backend gibt `{ error }` zurück, nicht `{ message }`)
- Login-Flow end-to-end verifiziert (register, login, `/me`, falsches Passwort → 401, kein Token → Weiterleitung)

## Aktueller Stand vs. Meilenstein-Plan

| Geplant (lt. Meilenstein) | Status | Anmerkung |
|---------------------------|--------|-----------|
| KW-21-Nachholung: Backend-Auth | ✅ | Express-Server, JWT, bcrypt, SQLite — vollständig funktionsfähig |
| Notiz-CRUD | ⬜ | Nächster Schritt diese Woche |
| Markdown-Editor + Live-Preview | ⬜ | |
| Ordner-System | ⬜ | |

## Nächste Schritte

- Notes-CRUD-Routen und -Model implementieren (`backend/src/routes/notes.js`, `backend/src/models/Note.js`)
- Dashboard von Demo-Daten auf echte `api.getNotes()`-Aufrufe umstellen
- Notiz-Detailseite speichern/laden verdrahten
- Ordner-System (Routen + Model + UI) implementieren

## AI-Einsatz diese Woche

| Tool/LLM | Zweck | Prompt (Zusammenfassung) | Ergebnis/Integration |
|-----------|-------|--------------------------|----------------------|
| Claude Code (CLI) | KW-21-Backfill: Backend-Auth + Frontend-Verdrahtung | `/batch`-Kommando → nach Audit serial ausgeführt: DB-Config, User-Model, JWT-Middleware, Auth-Routen, Express-Bootstrap, Frontend-Wiring | Alle Auth-Endpoints funktionsfähig; Login-Flow end-to-end verifiziert |
| Claude (Opus 4.7, Chat) | Konzeptuelles Verständnis der JWT-Authentifizierung | „Wie funktioniert ein JWT?“ → Aufbau (Header/Payload/Signature), Signatur-Prinzip; „Wie sende ich den Token bei einem Request mit?“ → `Authorization: Bearer`-Header + Express-Middleware | Verständnis des Auth-Flows vertieft; als Absicherung der implementierten JWT-Middleware und für die Präsentation genutzt |

## Commits diese Woche

| Datum | Commit-Message | Beschreibung |
|-------|----------------|--------------|
| 28.05. | feat(auth): Backend-Login vollständig implementiert + Frontend verdrahtet | Alle Backend-Auth-Stubs implementiert, Frontend verdrahtet, better-sqlite3 auf v12 aktualisiert |
