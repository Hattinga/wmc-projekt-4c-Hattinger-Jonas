# Wochenprotokoll KW 22 (26.05. – 01.06.2026)

## Was wurde gemacht?

- **KW-21-Backfill – Backend-Auth vollständig implementiert:**
  - Audit ergab: alle 12 Backend-Quelldateien waren reine TODO-Kommentar-Stubs (0 % implementiert)
  - `backend/src/config/database.js`: better-sqlite3-Verbindung, Schema-Bootstrap beim ersten Start (`IF NOT EXISTS`), `PRAGMA foreign_keys = ON`
  - `backend/src/models/User.js`: `create`, `findByEmail`, `findById`, `update`, `delete` mit prepared statements; SQLITE_CONSTRAINT_UNIQUE wird als `DUPLICATE`-Error weitergegeben
  - `backend/src/middleware/auth.js`: `Authorization: Bearer`-Header parsen, `jwt.verify` mit JWT_SECRET, `req.user` setzen, 401 bei fehlendem oder ungültigem Token
  - `backend/src/routes/auth.js`: `POST /register` (Validierung, bcrypt-Hash, User anlegen, JWT zurückgeben), `POST /login` (bcrypt.compare, JWT), `GET /me`, `PATCH /me`; Antwortformat immer `{ token, user }` ohne `password_hash`
  - `backend/src/index.js`: Express-App, CORS, dotenv, JSON-Body-Parser; nur `/api/auth` gemountet — andere Routen folgen in KW 22/23 wenn implementiert
  - `backend/package.json`: `dotenv ^16.4.0` ergänzt; `better-sqlite3` von v9 auf v12 aktualisiert (v12 hat prebuilt-Binaries für Node.js 24 — v9 schlägt auf diesem System ohne C++-Toolset fehl)
  - `backend/db/seed.sql`: Placeholder-bcrypt-Hash durch echten Hash für `password123` ersetzt
- **Frontend-Auth verdrahtet:**
  - `frontend/src/lib/stores/appState.svelte.js`: `currentUser: null` hinzugefügt; `locale` und `currentUser` werden beim Start aus `localStorage` geladen (war vorher nicht implementiert)
  - `frontend/src/routes/auth/+page.svelte`: `handleLogin`/`handleRegister` rufen jetzt `api.login`/`api.register` auf, schreiben Token + User in `localStorage`, setzen `appState.currentUser`
  - `frontend/src/routes/+layout.svelte`: `$effect`-Auth-Guard — leitet ohne Token auf `/auth` weiter
  - `frontend/src/lib/services/api.js`: Fehlerauswertung liest jetzt `err.error || err.message` (Backend gibt `{ error }` zurück, nicht `{ message }`)
- Login-Flow end-to-end verifiziert (register, login, `/me`, falsches Passwort → 401, kein Token → Weiterleitung)

- **Notes CRUD – vollständiger vertikaler Slice:**
  - `backend/src/models/Note.js`: `findAll(userId)`, `findById(id, userId)`, `create`, `update`, `delete` mit prepared statements; LEFT JOIN auf `folders` für Ordnernamen, Backlinks-Count via Subquery; `tags: []` als Platzhalter bis Tag-Management steht
  - `backend/src/routes/notes.js`: `GET /`, `GET /:id`, `POST /`, `PUT /:id`, `DELETE /:id`; alle mit `requireAuth`; Authorization: `userId`-Filter verhindert Zugriff auf fremde Notizen (gibt 404 statt 403 — kein Informationsleck)
  - `backend/src/index.js`: `/api/notes` gemountet
  - `frontend/src/routes/dashboard/+page.svelte`: DEMO_NOTES durch `$state([])` + `$effect(api.getNotes())` ersetzt; Sortierung + Suche unverändert; FAB ruft `api.createNote()` auf und navigiert zur neuen Notiz-ID; Avatar zeigt echte User-Initialen; leerer Zustand mit Hinweistext
  - `frontend/src/routes/note/[id]/+page.svelte`: DEMO_NOTES + DEMO_CONTENT (module context) entfernt; Note lädt per `api.getNote(noteId)` in `$effect`; debounced Autosave (800ms) per `api.updateNote()` in zweitem `$effect`; Speicher-Badge zeigt „Speichert…" / „Gespeichert" reaktiv
- Notes CRUD end-to-end verifiziert: POST 201, GET Liste, PUT aktualisiert Inhalt, Auth-Boundary (fremder User → 404), DELETE 204

- **Bugfix – `[[Wiki-Link]]`-Klick ohne Funktion:**
  - Ursache: `note/[id]/+page.svelte` übergab `notes={[]}` an `MarkdownEditor`; `renderMarkdown()` konnte keinen Titel auflösen → alle Links bekamen `href="#"`
  - Fix: `allNotes`-State ergänzt, `api.getNotes()` im Lade-`$effect` aufgerufen, `notes={allNotes}` an Editor weitergegeben

- **Ordner-System – vollständiger vertikaler Slice:**
  - `backend/src/models/Folder.js`: `findAll(userId)`, `findById(id, userId)`, `create`, `update`, `delete` mit prepared statements; Note-Count via Subquery
  - `backend/src/routes/folders.js`: `GET /`, `GET /:id`, `POST /`, `PUT /:id`, `DELETE /:id`; alle mit `requireAuth`; 404 statt 403 bei fremdem Ordner
  - `backend/src/index.js`: `/api/folders` gemountet
  - `frontend/src/lib/stores/appState.svelte.js`: `folders: []` und `tags: []` ergänzt
  - `frontend/src/lib/components/layout/Sidebar.svelte`: `$effect` lädt `api.getFolders()` bei eingeloggtem User; `buildTree()` baut Hierarchie aus flacher Liste (parent_id); Ergebnis in `appState.folders` gespeichert

## Aktueller Stand vs. Meilenstein-Plan

| Geplant (lt. Meilenstein) | Status | Anmerkung |
|---------------------------|--------|-----------|
| KW-21-Nachholung: Backend-Auth | ✅ | Express-Server, JWT, bcrypt, SQLite — vollständig funktionsfähig |
| Notiz-CRUD | ✅ | GET/POST/PUT/DELETE, Dashboard + Detailseite verdrahtet, Autosave |
| Markdown-Editor + Live-Preview | ✅ | War bereits aus KW 21, Editor-Komponente voll funktionsfähig |
| Ordner-System | ✅ | Folder.js Model, folders Routen, Sidebar lädt echte Ordner per API |

## Nächste Schritte (KW 23)

- `[[Note]]`-Syntax: `linkExtractor.js` server-seitig beim Speichern aufrufen, `note_links`-Tabelle befüllen
- Backlinks-Endpoint: `GET /api/notes/:id/backlinks` implementieren, `BacklinksPanel` verdrahten
- Graph-View (D3.js): `GET /api/graph` mit Nodes + Edges implementieren, `GraphView.svelte` auf echte Daten umstellen
- WebSocket (Socket.io): Echtzeit-Updates bei Note-Änderungen

## AI-Einsatz diese Woche

| Tool/LLM | Zweck | Prompt (Zusammenfassung) | Ergebnis/Integration |
|-----------|-------|--------------------------|----------------------|
| Claude Code (CLI) | KW-21-Backfill: Backend-Auth + Frontend-Verdrahtung | `/batch`-Kommando → nach Audit serial ausgeführt: DB-Config, User-Model, JWT-Middleware, Auth-Routen, Express-Bootstrap, Frontend-Wiring | Alle Auth-Endpoints funktionsfähig; Login-Flow end-to-end verifiziert |
| Claude (Opus 4.7, Chat) | Konzeptuelles Verständnis der JWT-Authentifizierung | „Wie funktioniert ein JWT?” → Aufbau (Header/Payload/Signature), Signatur-Prinzip; „Wie sende ich den Token bei einem Request mit?” → `Authorization: Bearer`-Header + Express-Middleware | Verständnis des Auth-Flows vertieft; als Absicherung der implementierten JWT-Middleware und für die Präsentation genutzt |
| Claude Code (CLI) | Notes CRUD + Ordner-System + Wiki-Link-Bugfix | Serial: Note.js Model, notes/folders Routen, Dashboard + Detailseite verdrahten, Autosave, wiki-link-Auflösung, Sidebar-Wiring, Folder.js Model | Alle CRUD-Endpoints funktionsfähig; Wiki-Links navigierbar; Sidebar lädt echte Ordner |
| Claude Code (CLI) — /batch + /plan | Bug-Fix-Sweep: 59 Audit-Funde (4 Critical, ~20 High, ~20 Medium, ~15 Low) | `/batch` Audit-Team → 59 Funde gemeldet; `/plan fixe das alles` → alle 59 Funde implementiert: XSS-Pipeline, Autosave-Race, rekursiver Trigger, 401-Auto-Logout, Auth-Flash, Folder-Ownership, Circular-Parent, Cross-Tab-Logout, Dynamic-SQL, DB-Indizes u.v.m. | Build ✓ sauber; `date.js`-Util neu; Commits stehen noch aus |

## Commits diese Woche

| Datum | Commit-Message | Beschreibung |
|-------|----------------|--------------|
| 28.05. | feat(auth): Backend-Login vollständig implementiert + Frontend verdrahtet | Alle Backend-Auth-Stubs implementiert, Frontend verdrahtet, better-sqlite3 auf v12 aktualisiert |
| 28.05. | feat(kw22): Notes CRUD + Ordner-System + Wiki-Link-Fix + Audit Team Bug-Scan fix | Folder.js Model + folders Routen + Sidebar-Wiring; Wiki-Link-Fix (allNotes an Editor); appState um folders/tags erweitert |
