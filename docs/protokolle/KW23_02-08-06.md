# Wochenprotokoll KW 23 (02.06. – 08.06.2026)

## Was wurde gemacht?

- **Link-Extraktion server-seitig:** `linkExtractor.js` implementiert – `syncLinks()` parsed `[[Wiki-Links]]` aus dem Content und aktualisiert die `note_links`-Tabelle bei jedem Save (Create + Update). Case-insensitives Titel-Matching, Links auf nicht-existierende Notizen werden ignoriert.
- **Backlinks:** Neuer Endpoint `GET /api/notes/:id/backlinks` liefert alle Notizen, die auf die aktuelle Notiz verlinken; `BacklinksPanel` im Editor an echte Daten angebunden.
- **Tags-Backend:** `Tag.js`-Model + `GET/POST/PUT/DELETE /api/tags`; `note_tags`-Join in den Note-Queries, sodass Notizen ihre Tags mitliefern.
- **Graph-API + D3-Graph:** `GET /api/graph` gibt Nodes + Edges aus `note_links` zurück (user-scoped). `GraphView.svelte` von SVG-Demo auf echte D3.js Force-Simulation umgebaut: Drag, Zoom/Pan, Node-Größe nach Verlinkungsgrad.
- **WebSocket-Integration:** Socket.io-Server mit JWT-Auth im Handshake, User-spezifische Rooms (`user:<id>`). Backend emittiert `note:updated`/`note:deleted` bei Save/Delete; Frontend (`websocket.js`) verbindet bei Login, trennt bei Logout, synct `appState.notes` live.
- **Settings-Seite verdrahtet:** Profil speichern (`PATCH /me`), Passwort ändern, Theme-Toggle.
- **Store erweitert:** `appState` um `wsConnected` und `notes` ergänzt.

## Aktueller Stand vs. Meilenstein-Plan

| Geplant (lt. Meilenstein) | Status | Anmerkung |
|---------------------------|--------|-----------|
| [[Note]]-Syntax + Backlinks | ✅ | Server-seitige Extraktion + Backlinks-Endpoint, im Smoke-Test (09.06.) verifiziert |
| Graph-View (D3.js) | ✅ | Force-Layout mit Drag, Zoom, Degree-Sizing |
| WebSocket-Integration | ✅ | JWT-Auth, Rooms pro User; bekanntes Manko: kein Event bei Note-*Erstellung* (Fix in KW 24) |

## Nächste Schritte

- Doku nachziehen (dieses Protokoll, AI-Log, Fortschritt) → erledigt am 09.06.
- WS-Event bei Note-Erstellung ergänzen
- Responsive Layout (Mobile), Tag-Zuweisung im Editor-UI
- Manuelle Test-Checkliste für die Abgabe

## AI-Einsatz diese Woche

| Tool/LLM | Zweck | Prompt (Zusammenfassung) | Ergebnis/Integration |
|-----------|-------|--------------------------|----------------------|
| Claude Code (CLI) | KW23-Features implementieren | Links/Backlinks/Tags/Graph/WS/Settings auf Basis der bestehenden Stubs implementieren | Vollständig übernommen, Details siehe AI_LOG Session 11 |

## Commits diese Woche

| Datum | Commit-Message | Beschreibung |
|-------|----------------|--------------|
| 03.06. | feat(kw23): implement links, backlinks, tags, graph, websocket, settings | Alle KW23-Features in einem Commit: syncLinks, Backlinks-Endpoint, Tags-CRUD, Graph-API, D3-Force-Graph, Socket.io mit JWT, Settings-Wiring |
