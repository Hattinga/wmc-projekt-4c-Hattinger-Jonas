// TODO: t()-Hilfsfunktion für i18n.
// Liest locale aus appState.$state, gibt den übersetzten String für einen Schlüssel zurück.
// Schlüssel als Dot-Notation: t('notes.newNote') → 'Neue Notiz' (DE) / 'New note' (EN).
// Fallback: gibt den Schlüssel zurück wenn Übersetzung fehlt.

// export function t(key) { ... }
