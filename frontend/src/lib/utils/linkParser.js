// TODO: Client-seitiger Parser für [[Note Title]]-Syntax.
// Exportiert:
//   extractLinks(content: string) → string[]  (Liste der verlinkten Titel)
//   replaceLinks(content, notes)  → string    (ersetzt [[Titel]] durch Markdown-Links)
// Wird von MarkdownEditor für Live-Preview verwendet.
