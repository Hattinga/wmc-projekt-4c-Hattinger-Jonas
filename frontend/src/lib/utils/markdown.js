// TODO: Markdown → HTML Parser via marked.js oder markdown-it.
// Konfiguriert mit: sanitize (XSS-Schutz), [[Note]]-Links zu <a href="/note/:id">,
// Syntax-Highlighting optional.
// Exportiert: renderMarkdown(content: string) → string (HTML)
