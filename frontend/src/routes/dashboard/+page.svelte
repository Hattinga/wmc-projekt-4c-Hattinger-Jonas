<!-- TODO: /dashboard – Hauptseite nach Login.
     Zeigt NoteList (gefiltert nach aktivem Ordner/Tag aus Sidebar).
     "Neue Notiz"-Button erstellt via POST /api/notes und navigiert zu /note/:id.
     Suche filtert die Liste client-seitig über searchQuery aus globalem $state. -->
<script>
</script>
