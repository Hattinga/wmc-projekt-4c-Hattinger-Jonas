<!-- TODO: /graph – Graph-View Seite.
     Lädt Daten via GET /api/graph (nodes + edges), rendert GraphView-Komponente (D3.js).
     Vollbild-Layout ohne Sidebar. Legende zeigt Farb-Kodierung.
     Klick auf Node navigiert zu /note/:id. -->
<script>
</script>
