<!-- TODO: /note/:id – Notiz-Editor.
     Lädt Notiz via GET /api/notes/:id, rendert MarkdownEditor + Toolbar + BacklinksPanel.
     Auto-Save mit Debounce (1s nach letzter Änderung) via PUT /api/notes/:id.
     Sendet WebSocket-Event 'note:updated' nach erfolgreichem Save.
     Zeigt Notiz-Titel als editierbares H1 über dem Editor. -->
<script>
</script>
